'''---
smart_queue:
  wan_interface: eth0
  enabled: true
  uplink_mbps: 100
  downlink_mbps: 100
  scheduler: fq_codel
  dscp_marking:
    ef:
      codepoint: 46
      enabled: true
  flows:
    - name: voip-ef
      match:
        dscp: 46
      priority: high
